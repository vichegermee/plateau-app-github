/* 'use strict';
describe('Controller: trombinoscopeCtrl', function () {

	  // load the controller's module
	  beforeEach(module('plateauAppApp'));

	  var $controller, $rootScope, scope, controller;


	  // Initialize the controller and a mock scope
	  beforeEach(inject(function (_$controller_) {
	
		  $controller=_$controller_;

		  

	  }));

	  it('show_fonction should be default false', function() {
		  
		 var controller =  $controller('trombinoscopeCtrl',{});
		 expect(controller.showFonction).toBe(false);
		});
	  
//	  it('Affichage du detail d\'un collaborateur.  $scope.show();', function() {
//		  var $scope = {};
//	      var controller = $controller('trombinoscopeCtrl', { $scope: $scope });
//	      this.show();
//	      expect(this.show_fonction).toBe(true);
//		});

		
	}); */